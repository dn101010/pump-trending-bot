# 🚀 Pump.fun Trending Bot

Telegram бот для мгновенных уведомлений о входе монет в трендинг на pump.fun (как в мобильном приложении!)

## ⚡ Особенности

- **Мгновенные уведомления** — проверка каждые 15 секунд
- **Только свежие монеты** — фильтр по времени создания (< 3 часов)
- **Без дубликатов** — умное отслеживание уже уведомлённых монет
- **Лёгкий деплой** — одна кнопка на Railway

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Создай Telegram бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Придумай имя боту (например: `Pump Trending Alert`)
4. Придумай username (должен заканчиваться на `bot`, например: `pump_trending_alert_bot`)
5. **Скопируй токен** — он выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### Шаг 2: Получи Chat ID

**Вариант А — Личный чат:**
1. Найди своего бота в Telegram и нажми `/start`
2. Открой в браузере: `https://api.telegram.org/bot<ТВОЙ_ТОКЕН>/getUpdates`
3. Найди поле `"chat":{"id":123456789` — это твой Chat ID

**Вариант Б — Канал:**
1. Добавь бота администратором в канал
2. Отправь любое сообщение в канал
3. Открой `https://api.telegram.org/bot<ТВОЙ_ТОКЕН>/getUpdates`
4. Chat ID канала начинается с `-100` (например: `-1001234567890`)

### Шаг 3: Деплой на Railway

[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/new/template?template=https://github.com/YOUR_USERNAME/pump-trending-bot)

> ⚠️ Замени `YOUR_USERNAME` на свой GitHub username после создания репозитория

**Или вручную:**

1. Зарегистрируйся на [railway.com](https://railway.com) через GitHub
2. Создай новый проект → **"Deploy from GitHub repo"**
3. Выбери репозиторий с ботом
4. Перейди во вкладку **Variables** и добавь:
   - `BOT_TOKEN` = токен от BotFather
   - `CHAT_ID` = ID чата или канала
   - `POLL_INTERVAL_MS` = `15000` (15 секунд)
5. Railway автоматически задеплоит — бот пришлёт сообщение в чат!

---

## 📋 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветственное сообщение |
| `/status` | Показать статус бота (uptime, отслеживаемые монеты) |
| `/check` | Принудительная проверка трендинга |

---

## ⚙️ Настройка

Все настройки через переменные окружения в Railway:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | **Обязательно** — токен от @BotFather | — |
| `CHAT_ID` | **Обязательно** — ID чата/канала | — |
| `POLL_INTERVAL_MS` | Интервал проверки (миллисекунды) | `15000` |

---

## 📁 Структура проекта

```
pump-trending-bot/
├── package.json      # Зависимости и скрипты
├── index.js          # Точка входа
├── .gitignore        # Игнорируемые файлы
├── README.md         # Этот файл
└── src/
    ├── bot.js        # Основной контроллер бота
    ├── monitor.js    # Мониторинг pump.fun API
    ├── tracker.js    # Отслеживание уведомлённых монет
    └── formatter.js  # Форматирование сообщений
```

---

## 🔧 Локальный запуск (для разработки)

```bash
# 1. Клонируй репозиторий
git clone https://github.com/YOUR_USERNAME/pump-trending-bot.git
cd pump-trending-bot

# 2. Установи зависимости
npm install

# 3. Создай .env файл (только для локального теста!)
echo "BOT_TOKEN=your_token_here" > .env
echo "CHAT_ID=your_chat_id_here" >> .env
echo "POLL_INTERVAL_MS=15000" >> .env

# 4. Запусти
npm start
```

---

## 🐛 Отладка

**Смотри логи в Railway:**
1. Открой проект в Railway
2. Перейди во вкладку **Deploy**
3. Кликни на **View Logs**

**Типичные проблемы:**

| Проблема | Решение |
|----------|---------|
| Бот не пишет в чат | Проверь `CHAT_ID` — для канала должен начинаться с `-100` |
| Нет уведомлений | Проверь логи — возможно API pump.fun недоступен |
| Ошибка 429 | Telegram rate limit — подожди несколько минут |

---

## 📝 Лицензия

MIT — делай что хочешь!

---

## 💬 Поддержка

По вопросам пиши в Telegram: [@your_username](https://t.me/your_username)

---

**Удачного трейдинга! 🚀📈**
